import * as clusters from './cluster.selectors';
import * as invites from './invite.selectors';
import * as terminations from './termination.selectors';

export const ClusterManagementSelectors = { clusters, invites, terminations };
