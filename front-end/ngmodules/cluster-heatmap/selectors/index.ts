import * as filter from './filter.selectors';
import * as data from './data.selectors';
import * as meters from './meters.selectors';
import * as measures from './measures.selectors';

export const HeatmapSelectors = { data, filter, meters, measures };
